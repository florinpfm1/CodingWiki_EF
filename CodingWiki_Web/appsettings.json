{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      //we configure the log level for the database commands to "Information" so we can see the SQL queries that are being executed
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "AllowedHosts": "*",
  //here we set the connection string to the database - we must use exactly "ConnectionStrings" as the key, this is where by default it will look for connection string value
  "ConnectionStrings": {
    "DefaultConnection": "Server=ACERFLORIN\\SQL2022SIIT;Database=CodingWikiData;TrustServerCertificate=True;Trusted_Connection=True"
  }
}
